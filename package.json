{
  "name": "multiplayer-checkers-game",
  "version": "1.0.0",
  "description": "A multiplayer checkers game with React and Node.js",
  "scripts": {
    "start": "cd server && yarn start",
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\"",
    "build": "cd client && yarn build",
    "render-postbuild": "cd client && yarn install && yarn build && cd ../server && yarn install && yarn build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
} 